(function (root, factory) { var api = factory(); if (typeof module === 'object' && module.exports) module.exports = api; else root.GearDimensionRenderer = api; })(typeof self !== 'undefined' ? self : this, function () {
  'use strict';
  function line(group, primitives, x1, y1, x2, y2, label) { group.appendChild(primitives.node('line', { x1: x1, y1: y1, x2: x2, y2: y2, class: 'dimension-line' })); group.appendChild(primitives.node('text', { x: (x1 + x2) / 2, y: (y1 + y2) / 2 - 6, 'text-anchor': 'middle', class: 'geometry-dimension' }, label)); }
  function stage(group, item, primitives) { var geometry = item.stage.geometry || {}, y = item.y + item.diameter / 2 + 25; if (Number.isFinite(item.centerDistance) && item.centerDistance > 0) line(group, primitives, item.x, y, item.x + item.centerDistance, y, 'c = ' + item.centerDistance.toFixed(2) + ' mm'); var moduleValue = item.stage.parameters && item.stage.parameters.module; if (Number.isFinite(moduleValue)) group.appendChild(primitives.node('text', { x: item.x, y: y + 20, class: 'geometry-dimension' }, 'm = ' + moduleValue.toFixed(2) + ' mm')); }
  function envelope(group, layout, primitives) { var b = layout.bounds; group.appendChild(primitives.node('rect', { x: 12, y: 12, width: b.width - 24, height: b.height - 24, class: 'geometry-envelope' })); group.appendChild(primitives.node('text', { x: b.width / 2, y: b.height - 18, 'text-anchor': 'middle', class: 'geometry-dimension' }, 'Encombrement ' + b.width.toFixed(0) + ' × ' + b.height.toFixed(0) + ' mm')); }
  return { stage: stage, envelope: envelope };
});
