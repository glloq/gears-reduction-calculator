const {test,expect}=require('@playwright/test');
let errors=[];
test.beforeEach(async({page})=>{errors=[];page.on('console',m=>{if(m.type()==='error')errors.push(m.text());});page.on('pageerror',e=>errors.push(e.message));await page.goto('/');});
test.afterEach(()=>expect(errors,'browser errors').toEqual([]));
test('default search selects a solution rendered from calculated geometry',async({page})=>{await page.getByRole('button',{name:'Rechercher'}).click();await expect(page.locator('.solution-tile')).not.toHaveCount(0,{timeout:20000});await expect(page.locator('.solution-tile').first()).toHaveClass(/selected/);await expect(page.locator('#svgContainer .train-svg')).toBeVisible();await expect(page.locator('.train-stage').first()).toHaveAttribute('data-center-distance-mm',/\d/);await expect(page.getByRole('button',{name:'Animer'})).toBeEnabled();await page.getByRole('button',{name:'Animer'}).click();await expect(page.locator('.train-svg')).toHaveClass(/is-animated/);});
test('need derives 12:1 and returns an output near 125 rpm',async({page})=>{await page.locator('#objective_mode').selectOption('need');await page.locator('#vitesse_entree').fill('1500');await page.locator('#rpm_sortie_cible').fill('125');await expect(page.locator('#derivedRatio')).toContainText('12.00');await page.getByRole('button',{name:'Rechercher'}).click();await expect(page.locator('.solution-tile').first()).toBeVisible({timeout:20000});await expect(page.locator('#solutionCard')).toContainText('125.0 rpm');});
test('linear UI and constraints reach the rack solver',async({page})=>{await page.locator('#objective_mode').selectOption('rotationTranslation');await expect(page.locator('#type-rack')).toBeChecked();await expect(page.locator('#type-spur')).toBeHidden();await page.locator('#linear_travel_per_rev').fill('62.83');await page.getByRole('button',{name:'Rechercher'}).click();await expect(page.locator('.solution-tile').first()).toContainText('Course',{timeout:20000});await expect(page.locator('#solutionCard')).toContainText('Vitesse linéaire');await expect(page.locator('.train-stage.rack .rack-teeth')).toBeVisible();const force=Number((await page.locator('#solutionCard').textContent()).match(/Force sortie([\d.]+)/)?.[1]);await page.locator('#linear_force_min').fill(String(force+100000));await page.getByRole('button',{name:'Rechercher'}).click();await expect(page.locator('.solution-tile')).toHaveCount(0,{timeout:20000});});
